name: PR Check

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["**"]

jobs:
  pr_check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3      
          
      - name: test open
        run: open README.md
