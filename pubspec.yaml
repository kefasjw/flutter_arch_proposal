name: flutter_arch_proposal

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  agent:
    path: agent
  auth:
    path: auth
  auth_core:
    path: auth_core
  core:
    path: core
  counter:
    path: counter
  flutter:
    sdk: flutter
  settings:
    path: settings
  shared_dependency:
    path: shared_dependency

dev_dependencies:
  build_runner: ^2.3.2
  flutter_test:
    sdk: flutter
  freezed: ^2.3.2

flutter:
  uses-material-design: true
